spring:
  datasource:
    url: jdbc:mysql://localhost:3306/learning_platform
    username: root
    password: sql
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
